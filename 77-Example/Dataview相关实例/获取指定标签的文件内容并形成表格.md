```dataviewjs
//获取指定标签的文件内容并形成表格
let pages = dv.pages("#紧急任务");
let result = [];
for (let page of pages) {
 let content =await dv.io.load(page.file.link)
result.push([page.file.link,content.substr(0,200) + '...' ]);
}

dv.table(["File", "Contents"], result);

```